import ResponsesPageClient from "@/components/responses/ResponsesPageClient";

/**
 * Responses Page
 * 
 * Displays RSVP responses from Google Sheets via Apps Script
 */
export default function ResponsesPage() {
  return <ResponsesPageClient />;
}